Resources:

  AWS::DirectConnect::Connection:
    Icon:
      Name: networking-content-delivery/direct-connect

  AWS::DirectConnect::Gateway:
    Icon:
      Name: networking-content-delivery/direct-connect-gateway
